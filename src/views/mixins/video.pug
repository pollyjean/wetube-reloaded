mixin video(video)
  article
    h4
      a(href=`/videos/${video.id}`)=video.title
    p=video.desc
    div
      each hashtag in video.hashtags
        i=hashtag + " "
    small Posted #{video.createdAt}.

  hr